# Copy this file to .env and fill in your own key.
# Used only by the local dev server (server/rulesApiPlugin.ts) to call the
# Anthropic API for parsing a book's rules text — it never reaches the browser.
ANTHROPIC_API_KEY=
